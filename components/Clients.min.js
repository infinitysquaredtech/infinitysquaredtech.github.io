function Clients() {
    // Sample client data - replace with actual client logos
    const clients = [
        { name: "Tech Triq", logo: "asset/clients/logo-techtriq.webp" },
        { name: "Ascend", logo: "asset/clients/logo-ascend.webp" },
        { name: "PixelFarm", logo: "asset/clients/logo-pixelfarm.webp" },
        { name: "Render3D", logo: "asset/clients/logo-render3d.webp" },
        { name: "PKT", logo: "asset/clients/logo-pkt.webp" },
        { name: "Ambit", logo: "asset/clients/logo-ambit.webp" },
    ];

    try {
        return (
            <section id="clients" data-name="clients" data-file="components/Clients.min.js" className="py-20 bg-black">
                <div className="container mx-auto px-6">
                    <div className="text-center mb-16">
                        <h2 className="text-4xl md:text-4xl font-bold text-white mb-4">
                            Trusted by Industry Leaders
                        </h2>
                        <p className="text-xl text-white max-w-3xl mx-auto">
                            We're proud to partner with innovative companies across various industries
                        </p>
                    </div>

                    <div className="relative overflow-hidden">
                        <div className="flex animate-scroll">
                            {/* First set of logos */}
                            {clients.map((client, index) => (
                                <div
                                    key={`first-${index}`}
                                    className="flex-shrink-0 mx-8 w-32 h-16 md:w-40 md:h-20 flex items-center justify-center"
                                >
                                    <img
                                        src={client.logo}
                                        alt={`${client.name} logo`}
                                        className="max-w-full max-h-full object-contain filter grayscale hover:grayscale-0 transition-all duration-300 opacity-70 hover:opacity-100"
                                        onError={(e) => {
                                            // Fallback to text if image fails to load
                                            e.target.style.display = 'none';
                                            e.target.nextSibling.style.display = 'flex';
                                        }}
                                    />
                                    <div 
                                        className="hidden items-center justify-center w-full h-full bg-gray-100 rounded-lg text-gray-600 font-semibold text-sm"
                                        style={{ display: 'none' }}
                                    >
                                        {client.name}
                                    </div>
                                </div>
                            ))}
                            {/* Duplicate set for seamless loop */}
                            {clients.map((client, index) => (
                                <div
                                    key={`second-${index}`}
                                    className="flex-shrink-0 mx-8 w-32 h-16 md:w-40 md:h-20 flex items-center justify-center"
                                >
                                    <img
                                        src={client.logo}
                                        alt={`${client.name} logo`}
                                        className="max-w-full max-h-full object-contain filter grayscale hover:grayscale-0 transition-all duration-300 opacity-70 hover:opacity-100"
                                        onError={(e) => {
                                            e.target.style.display = 'none';
                                            e.target.nextSibling.style.display = 'flex';
                                        }}
                                    />
                                    <div 
                                        className="hidden items-center justify-center w-full h-full bg-gray-100 rounded-lg text-gray-600 font-semibold text-sm"
                                        style={{ display: 'none' }}
                                    >
                                        {client.name}
                                    </div>
                                </div>
                            ))}
                        </div>
                    </div>
                </div>
            </section>
        );
    } catch (error) {
        console.error('Clients component error:', error);
        reportError(error);
        return (
            <section className="py-20 bg-black">
                <div className="container mx-auto px-6 text-center">
                    <p className="text-gray-500">Unable to load clients section</p>
                </div>
            </section>
        );
    }
}
