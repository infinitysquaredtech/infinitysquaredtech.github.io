function Clients() {
    const clients = [
        { name: "SBI", logo: "asset/clients/logo-sbi.webp" },
        { name: "Ambit Developers", logo: "asset/clients/logo-ambit.webp" },
        { name: "Rajshree Builders", logo: "asset/clients/logo-rajshree.webp" },
        { name: "Signia Developers", logo: "asset/clients/logo-signia.webp" },
        { name: "Radius Architects", logo: "asset/clients/logo-radiusarch.webp" },
        { name: "Accurina", logo: "asset/clients/logo-accurina.webp" },
        { name: "SP Bazaar", logo: "asset/clients/logo-spbazaar.webp" },
        { name: "Awesome Agrizone", logo: "asset/clients/logo-awesomeagrizone.webp" },
        { name: "Teknik Enterprises", logo: "asset/clients/logo-teknik.webp" },
        { name: "Nimasu", logo: "asset/clients/logo-nimasu.webp" }
    ];

    const partners = [
        { name: "Ascend", logo: "asset/partners/logo-ascend.webp" },
        { name: "PixelFarm", logo: "asset/partners/logo-pixelfarm.webp" },
        { name: "Render3D", logo: "asset/partners/logo-render3d.webp" },
        { name: "Tech Triq", logo: "asset/partners/logo-techtriq.webp" },
        { name: "PKT", logo: "asset/partners/logo-pkt.webp" },
    ];

    const tabContent = {
        clients: {
            heading: "Trusted by Industry Leaders",
            subtitle: "Proud to have delivered exceptional results for leading companies"
        },
        partners: {
            heading: "Strategic Partners and Collaborators",
            subtitle: "Companies we work alongside to build and deliver great solutions"
        }
    };

    try {
        const [activeTab, setActiveTab] = React.useState('clients');
        const activeList = activeTab === 'clients' ? clients : partners;

        const LogoStrip = ({ list }) => (
            <div className="relative overflow-hidden tab-content-fade">
                <div className="flex animate-scroll">
                    {list.map((item, index) => (
                        <div
                            key={`first-${index}`}
                            className="flex-shrink-0 mx-8 w-32 h-16 md:w-40 md:h-20 flex items-center justify-center"
                        >
                            <img
                                src={item.logo}
                                alt={`${item.name} logo`}
                                className="max-w-full max-h-full object-contain filter grayscale hover:grayscale-0 focus:grayscale-0 active:grayscale-0 transition-all duration-300 opacity-70 hover:opacity-100"
                                tabIndex={0}
                                onTouchStart={e => e.currentTarget.classList.remove('grayscale')}
                                onTouchEnd={e => e.currentTarget.classList.add('grayscale')}
                                onError={(e) => {
                                    e.target.style.display = 'none';
                                    e.target.nextSibling.style.display = 'flex';
                                }}
                            />
                            <div
                                className="hidden items-center justify-center w-full h-full bg-gray-100 rounded-lg text-gray-600 font-semibold text-sm"
                                style={{ display: 'none' }}
                            >
                                {item.name}
                            </div>
                        </div>
                    ))}
                    {/* Duplicate set for seamless loop */}
                    {list.map((item, index) => (
                        <div
                            key={`second-${index}`}
                            className="flex-shrink-0 mx-8 w-32 h-16 md:w-40 md:h-20 flex items-center justify-center"
                        >
                            <img
                                src={item.logo}
                                alt={`${item.name} logo`}
                                className="max-w-full max-h-full object-contain filter grayscale hover:grayscale-0 focus:grayscale-0 active:grayscale-0 transition-all duration-300 opacity-70 hover:opacity-100"
                                tabIndex={0}
                                onTouchStart={e => e.currentTarget.classList.remove('grayscale')}
                                onTouchEnd={e => e.currentTarget.classList.add('grayscale')}
                                onError={(e) => {
                                    e.target.style.display = 'none';
                                    e.target.nextSibling.style.display = 'flex';
                                }}
                            />
                            <div
                                className="hidden items-center justify-center w-full h-full bg-gray-100 rounded-lg text-gray-600 font-semibold text-sm"
                                style={{ display: 'none' }}
                            >
                                {item.name}
                            </div>
                        </div>
                    ))}
                </div>
            </div>
        );

        return (
            <section id="clients" data-name="clients" data-file="lib/components/Clients.min.js" className="py-20 bg-black relative overflow-hidden">
                <div className="glass-orb-container">
                    <div className="glass-orb glass-orb-clients-1"></div>
                    <div className="glass-orb glass-orb-clients-2"></div>
                </div>
                <div className="container mx-auto px-6 relative z-10">
                    <div className="text-center mb-10">
                        <h2 className="text-4xl md:text-4xl font-bold text-white mb-4">
                            {tabContent[activeTab].heading}
                        </h2>
                        <p className="text-xl text-white max-w-3xl mx-auto mb-8">
                            {tabContent[activeTab].subtitle}
                        </p>

                        {/* Tab switcher */}
                        <div className="inline-flex glass-tab-switcher rounded-full p-1 relative">
                            <span className={`glass-tab-indicator${activeTab === 'partners' ? ' active' : ''}`}></span>
                            <button
                                onClick={() => setActiveTab('clients')}
                                className={`relative z-10 px-6 py-2 rounded-full text-sm font-semibold cursor-pointer transition-colors duration-300 focus:outline-none ${
                                    activeTab === 'clients' ? 'text-black' : 'text-gray-300 hover:text-white'
                                }`}
                            >
                                Our Clients
                            </button>
                            <button
                                onClick={() => setActiveTab('partners')}
                                className={`relative z-10 px-6 py-2 rounded-full text-sm font-semibold cursor-pointer transition-colors duration-300 focus:outline-none ${
                                    activeTab === 'partners' ? 'text-black' : 'text-gray-300 hover:text-white'
                                }`}
                            >
                                Our Partners
                            </button>
                        </div>
                    </div>

                    <div>
                        <LogoStrip list={activeList} />
                    </div>
                </div>
            </section>
        );
    } catch (error) {
        console.error('Clients component error:', error);
        reportError(error);
        return (
            <section className="py-20 bg-black">
                <div className="container mx-auto px-6 text-center">
                    <p className="text-gray-500">Unable to load clients section</p>
                </div>
            </section>
        );
    }
}
